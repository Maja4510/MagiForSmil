---
import SecondaryButton from '../components/SecondaryButton.astro';
---
<div class="grid_1-1">
<div>
<img src="/public/img/hatogstav.png" alt="hat" class="hat">
</div>
<div class="sectionfourtext">
<h1>For kun 10 kroner</h1>
<p>Kan du hjælpe med at <span class="highlight">skabe magi.</span> Med din donation, kan vi skabe øjeblikke, hvor sygdom og bekymringer træder i baggrunden, og hvor fantasien og <span class="highlight">smilet</span> for lov at blomstre. 
</p>
<br>
<div id="stot">
<SecondaryButton>Støt nu →</SecondaryButton>
</div>
</div>
</div>
<img src="/public/img/quote.png" alt="quote">

<style>

    img {
        width: 100%;
    }

    .grid_1-1 {
        display: grid;
        grid-template-columns: 1fr 1fr;

    }

    .sectionfourtext {
    text-align: center; 
    margin: 0 auto; 
    display: flex;
    flex-direction: column;
    align-items: center; 
    justify-content: center; 
    width: 60ch;
}

h1 {
    color: #000;
   margin-left: 8%;
   font-family: IonicMTW04-Regular;
   font-size: 42px;
   font-style: normal;
   font-weight: 400;
   line-height: 120%; /* 50.4px */
   }
   
   p {
    color: #000;
text-align: center;
font-family: "Helvetica Neue";
font-size: 16px;
font-style: normal;
font-weight: 300;
line-height: 160%; /* 25.6px */
   }
    .highlight {
color: #E56F00;
font-family: "Helvetica Neue";
font-size: 16px;
font-style: normal;
font-weight: 700;
line-height: 160%;
}
   


img {
    width: 100%;
}

/* Endast hat-bilden får fade-effekten */
.hat {
    opacity: 0;
    transition: opacity 2s ease-in;
}

.fade-in {
    opacity: 1;
}
</style>


<script>
const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('fade-in');
            observer.unobserve(entry.target);
        }
    });
}, {
    threshold: 0.2
});

// Observera endast hat-bilden
document.addEventListener('DOMContentLoaded', () => {
    const hatImage = document.querySelector('.hat'); // eller '#hat' om du använder ID
    if (hatImage) {
        observer.observe(hatImage);
    }
});

document.addEventListener('DOMContentLoaded', () => {
    const stot = document.getElementById('stot');
    if (stot) {
        stot.addEventListener('click', () => {
            window.scrollTo({
                top: 6400, // Scrolla alltid till 200px från toppen
                behavior: 'smooth' // Mjuk scroll
            });
        });
    }
});
</script>



</style>